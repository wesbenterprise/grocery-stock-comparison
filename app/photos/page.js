import PhotoLibrary from './PhotoLibrary';

export const metadata = {
  title: 'Photo Library — Internal',
  description: 'Staging page for Publix historical photos',
};

export default function Page() {
  return <PhotoLibrary />;
}
